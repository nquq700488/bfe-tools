# 规范治理（唯一来源）

> 目的：避免规范重复、职责漂移、入口分叉。
> 原则：一类规则只保留一个权威来源，其他文件只摘要或链接。

## 职责边界对照

### 入口层

| 文件 | 唯一职责 | 不应承载 |
|------|---------|---------|
| `../AGENTS.md` | 项目入口：身份、铁律、启动协议、协作红线、交付前最低验证、规范导航索引 | 详细任务路由、完整工作流、完整协作流程、领域规范正文 |
| `task-routing.md` | 任务类型 → 最小必读集 | 原理解释、完整规则正文、流程方法论 |

### 流程层

| 文件 | 唯一职责 | 不应承载 |
|------|---------|---------|
| `workflow.md` | 本地执行方法论：理解、拆解、执行、验证、收尾 | 模式判断细则、CCB 协作注入策略、角色映射表 |
| `execution-mode-guidelines.md` | 完整模式 / 轻量模式判断；并行 / 串行分析 | 多模型注入策略、通用方法论、质量清单 |
| `collaboration.md` | 何时引入外部模型；如何通过 CCB 协作；降级策略 | 本地工作方式、模式判断正文、领域编码规范 |
| `.ccb/ccb.config` | CCB agent → provider / 模型配置唯一来源 | 角色职责、调用流程、注入策略 |
| `agent-registry.md` | 本地 Agent 角色索引；CCB agent 名称的角色语义 | provider / 模型配置、调用流程、注入策略 |

### 质量层

| 文件 | 唯一职责 | 不应承载 |
|------|---------|---------|
| `quality-checklist.md` | 性能、安全与实现质量约束 | `lint` / `vue-tsc` / 测试流程正文、模式判断、协作说明 |
| `git-workflow.md` | 提交与 Git 流程 | 编码规范正文、协作机制 |
| `testing-guidelines.md` | 测试编写、验证约束、验收标准格式 | 模式判断、协作注入策略 |

### 领域规范层

| 文件 | 唯一职责 | 不应承载 |
|------|---------|---------|
| `coding-standards/common.md` | 通用编码约束 | 组件专属规范、协作机制、工作流判断 |
| `coding-standards/typescript.md` | TypeScript 特定约束 | 其他语言规范、协作机制 |
| `component-guidelines.md` | Vue 组件约束 | Store / API / JSBridge 正文 |
| `styling-guidelines.md` | 样式与 CSS 约束 | 组件结构规范、协作流程 |
| `state-management-guidelines.md` | Pinia Store 约束 | API 规范、组件规范 |
| `api-guidelines.md` | API 请求与数据交互约束 | Store 规范、组件规范 |
| `utils-guidelines.md` | Composable / Utils 约束 | API 正文、组件正文 |
| `tool-system-guidelines.md` | 涂鸦工具系统约束 | 通用编码规范全文 |
| `native-bridge-guidelines.md` | 原生桥接约束 | API 规范全文、协作机制 |
| `ui-design-spec.md` | UI 设计契约 | 通用样式实现细节全文 |

### 记忆与维护层

| 文件 | 唯一职责 | 不应承载 |
|------|---------|---------|
| `task-memory.md` | `TASK_MEMORY.md` 写法与精简规则；仅定义何时写，不定义默认必读 | 项目流程、编码规范正文、会话启动必读要求 |
| `ai-memory.md` | `AI_MEMORY.md` 写法与长期经验沉淀触发规则 | 任务路由、短期任务状态、编码规范正文 |
| `context-compression-guidelines.md` | 长对话压缩规则 | 任务路由、编码规范正文 |
| `spec-governance.md` | 规范治理规则、职责边界、写作与合并准则 | 具体业务实现规范 |

## 新增规范前检查

- 先判断现有文件是否已有同类职责，避免新增重复源
- 若只是补细节，优先修改原文件，不新建
- 新文件必须能回答”它是某类规则的唯一来源吗”
- 若不能成为唯一来源，不应新建
- **规则的新增和重大修改**走 `continuous-learning.md` 的进化流程（模式提取 → 稳定性验证 → 固化）
- **规则的微调和纠错**走本文档的修改检查清单即可

## 修改规范时检查

- 同一条规则是否已在别处完整出现
- 新增内容是否越过当前文件职责边界
- 入口文件是否只保留摘要，不复制正文
- 交叉引用是否改为链接，而不是再次展开

## 写作与维护准则

- 规范读者是 Agent，优先约束表达，避免教程化叙述
- 同类规则只保留一处完整正文，其他文档只链接
- 示例最小化；必要时用“正确/错误”对照
- 结构保持扁平，避免多层嵌套导致读取成本上升
