{
  "name": "bfe-tools-desktop",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:embed": "cp src-tauri/tauri.embed.conf.json src-tauri/tauri.conf.json && BFE_EMBED=1 tauri build && cp src-tauri/tauri.url.conf.json src-tauri/tauri.conf.json"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.3.0"
  }
}
